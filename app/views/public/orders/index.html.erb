<div class="col-md-8 offset-md-1">
  <h2>注文履歴一覧</h2>
  <table class="table table-hobver table-inverse">
    <thead>
      <tr>
        <th>注文日</th>
        <th>配送先</th>
        <th>注文商品</th>
        <th>支払金額</th>
        <th>注文詳細</th>
        <th colspan="5"></th>
      </tr>
    </thead>

    <% @items.each do |item| %>
      <tr>
        <td><%= @customer.datetime %></td>
        <td>
          <%= @custmer.postal_code %></br>
          <%= @custmer.address %>
        </td>
        <td>
          <%= @customer.item_oreder %>
        </td>
        <td>
          <%= @customer.total_price %>
        </td>
        <td>
          <%= link_to '表示する',order_path, method: :get, class: "btn btn-secondary" %>
        </td>
      </tr>
    <% end %>
  </table>
</div>