<div class="col-md-8 offset-md-1">
  <h2>
    <%= link_to admin_customer_path(@customer.id) do %>
      <%= @customer_firtst_name %><%= @customer_last_name %>さんの注文履歴
    <% end %>
  </h2>
  <table class="table table-hobver table-inverse">
    <thead>
      <tr>
        <th>購入日時</th>
        <th>注文個数</th>
      </tr>
    </thead>
      <% @customer_orders.each do |order| %>
        <tr>
          <td>
            <%= link_to admin_order_path(item.customer.id) do %>
              <%= @item_oreder.datetime %>
            <% end %>
          </td>
          <td>
            <%= @item.amount %>
          </td>
        </tr>
      <% end %>
    </thead>
  </table>
  <%= paginate orders %>
</div>